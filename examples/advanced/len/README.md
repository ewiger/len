# len2len

This advanced example develops `level 1` parser and more to be used in len-cli. Essentially this is equivalent to an attempt to for a `compiler to compile itself`. But here we are dealing with meta language, hence we code generate the parser and validator for `len.l1` using `len.l1` itself. This is a powerful demonstration of the expressiveness and self-hosting capabilities of `len`.