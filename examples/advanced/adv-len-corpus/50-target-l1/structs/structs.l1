type Real

struct Point
    x: Real
    y: Real

struct Interval
    start: Real
    end: Real

spec interval_well_formed
    given i: Interval
    must exists s: Real, e: Real .
        Interval_start(i, s) and Interval_end(i, e) and s <= e