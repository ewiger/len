quasi:
    let n := length(xs)

    for i in 1 .. n - 1:
        let key := xs[i]
        let j := i - 1

        while j >= 0 and xs[j] > key:
            set xs[j + 1] := xs[j]
            set j := j - 1

        set xs[j + 1] := key

    return xs